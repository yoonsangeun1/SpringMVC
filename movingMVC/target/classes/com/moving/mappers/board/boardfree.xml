<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="BoardFree">

<resultMap type="NormalPostDTO" id="boardInfo">
	<association property="normalPostVO" javaType="com.moving.domain.NormalPostVO" autoMapping="true"/>
	<association property="mUserVO" javaType="com.moving.domain.MUserVO" autoMapping="true"/>
	<!-- <collection property="files" javaType="FileVO" autoMapping="true"/> -->
</resultMap>
 
 <!-- 게시물 개수 -->
 <select id="bf_count" resultType="int">
  SELECT COUNT(*) FROM NORMAL_POST
 </select>
 
 <!-- 페이징 -->
 <select id="bf_list" resultMap="boardInfo">
SELECT * FROM
(SELECT ROWNUM rNum, N.ID, N.TITLE,
 U.NICKNAME, N.REGISTER_DATE, N.HIT
FROM NORMAL_POST N, M_USER U
WHERE N.USER_ID = U.ID
ORDER BY N.ID DESC)
WHERE rNum &gt;= #{startrow}
AND rNum &lt;= #{endrow}

<!--   select * from 
  (select np.rowNum rNum, np.id, np.user_id, np.title,
  np.content, np.hit, np.register_date, mu.nickname 
  from
  (select * from normal_post np,m_user mu order by np.id desc))
  where rNum &gt;= #{startrow} and rNum &lt;= #{endrow} -->
  
  <!-- rowNum컬럼은 오라클에서 테이블 생성시 생성되는
  기본컬럼으로 레코드 저장시 일련번호값이 저장됨. rNum
  rowNum컬럼의 별칭명. &gt;은 >, &lt; < , 1이상
  10이하 사이의 10개목록만 검색 -->
  
 </select>
 
 <!-- 글쓰기 -->
 <insert id="bf_in">
  INSERT INTO NORMAL_POST (ID,USER_ID,TITLE,CONTENT) 
  VALUES(NORMAL_POST_SEQ.NEXTVAL,#{userId},#{title},
  #{content})
 </insert>
 
 <!-- 조회수 증가 -->
 <update id="bf_hit">
  UPDATE NORMAL_POST 
  SET HIT=HIT+1 
  WHERE ID=#{id}
 </update>
 
 <!-- 내용보기 -->
 <select id="bf_cont" resultMap="boardInfo">
  SELECT N.*, M.nickname
  FROM NORMAL_POST N, M_USER M
  WHERE N.USER_ID = M.ID
  AND N.ID=#{id}
 </select>
 
 

 
 <!-- 내용수정 -->  <!-- session 본인 닉네임으로만 수정 가능하게 해야함. -->
 <update id="bf_edit">
 UPDATE NORMAL_POST 
 SET TITLE=#{title},
   	 CONTENT=#{content} 
 WHERE ID=#{id}   
 </update>
 
 <!-- 게시글 삭제 --> <!-- 본인만 본인 게시글 삭제 가능하게 해야함 -->
 <delete id="bf_del">
  DELETE FROM NORMAL_POST 
  WHERE ID=#{id}
 </delete>
 
 
<!--  <resultMap type="bf" id="bf_result"> type는 mybatis에서 쓴 것
 normal_postVO는 1개 이지만 xml은 여러개 해도 됨? ㅇㅋ
  <id property="id" column="id" /> 컬럼명은 db컬럼명과 동일해야함, 프로퍼티는 노상관
  <result property="title" column="title" />
  <association property="user_id" column="user_id" javaType="mu" />
 </resultMap> -->
 

 </mapper>




